<%= form_with(model: order_information) do |form| %>
  <% if order_information.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(order_information.errors.count, "error") %> prohibited this order_information from being saved:</h2>

      <ul>
        <% order_information.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :order_id, style: "display: block" %>
    <%= form.text_field :order_id %>
  </div>

  <div>
    <%= form.label :status, style: "display: block" %>
    <%= form.number_field :status %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
