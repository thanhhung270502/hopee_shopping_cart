<%= form_with(model: user_voucher) do |form| %>
  <% if user_voucher.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(user_voucher.errors.count, "error") %> prohibited this user_voucher from being saved:</h2>

      <ul>
        <% user_voucher.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
